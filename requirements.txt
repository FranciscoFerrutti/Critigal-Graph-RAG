# Datos y utilidades
pandas>=2.0
requests>=2.31
pyyaml>=6.0
python-dotenv>=1.0
pyarrow>=14.0                    # parquet de nodos/relaciones/embeddings
pydantic

# Neo4j
neo4j>=5.14                      # driver oficial (soporta vector index)

# LangChain como capa transversal de abstracción
langchain>=0.3
langchain-core>=0.3
langchain-google-genai>=2.0      # Gemini (embeddings + chat) — proveedor default
langchain-neo4j>=0.3             # Neo4jVector, GraphCypherQAChain
langchain-huggingface>=0.1       # fallback local de embeddings
langgraph>=0.2                   # agente planificador (etapa 3)

# Embeddings locales (usados solo si se selecciona provider=huggingface)
sentence-transformers>=2.7
torch>=2.0

# Web (etapa 3)
fastapi>=0.111
uvicorn[standard]>=0.29
jinja2>=3.1

# Testing y calidad
pytest>=8.0
ruff>=0.4

# Jupyter (exploración interactiva)
jupyter>=1.0
ipykernel>=6.0
